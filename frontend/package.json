{
  "name": "claw-frontend",
  "private": true,
  "workspaces": [
    "packages/*",
    "app"
  ],
  "scripts": {
    "test:unit": "vitest run --config packages/claw-core/vitest.config.ts && vitest run --config packages/claw-ui/vitest.config.ts && vitest run --config app/vitest.config.ts",
    "test:e2e": "npx playwright test",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "jsdom": "^28.1.0",
    "vitest": "^4.0.18"
  }
}
