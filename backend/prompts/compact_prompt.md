你正在为**同一个 AI Agent 的后续上下文**生成交接摘要。
由于上下文窗口限制，中间的对话将被压缩，你的摘要将成为后续推理的唯一参考。

请按以下结构输出：

## 任务目标
用户的原始需求是什么。

## 已完成的工作
- 调用了哪些工具、关键结果（数值、状态、文件路径）
- 做出了哪些决策、为什么

## 待完成的工作
- 如果有未完成的步骤，列出来
- 如果任务已完成，写"无"

## 关键数据
- 必须保留的 ID、金额、日期、文件路径等

规则：
- 用中文输出
- 不超过 400 字
- 不要编造未发生的事情
- 工具输出的具体内容不需要复制，只保留关键结论
